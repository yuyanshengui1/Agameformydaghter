# 儿童益智大富翁游戏开发说明书

## 项目概述

一款面向 6 岁女孩的儿童益智大富翁游戏，类似经典大富翁（Monopoly），在 Windows 电脑网页浏览器上直接运行，无需安装。通过掷骰子让角色在棋盘上走格子，结合算术、英语、语文三类益智题目，答对即可获得地块、盖房子升级，寓教于乐。

- **目标用户**：6 岁小女孩
- **运行平台**：Windows 桌面浏览器（Chrome / Edge）
- **核心特色**：益智题目 + 大富翁经营玩法 + 童趣视觉风格
- **技术栈**：Phaser 3 + TypeScript + Vite

---

## 一、游戏玩法规则

### 1.1 棋盘结构

- 总共 **44 个地块**，围成一个正方形。
- 不算四个角上的地块，每边 10 个地块，所以 `4 × 10 + 4 = 44`。
- 四个角分别为特殊地块（起点、休息、监狱/传送等）。

### 1.2 角色与回合

- 支持 **2～4 人** 游戏，第一个玩家为人类，其余为 AI。
- 玩家可选择不同角色形象（动物卡通：小兔子、小狐狸、小熊、小猫咪）。
- 每回合掷两颗骰子（1～6），按点数之和前进。
- **掷出双数（两个骰子相同）可再掷一次**，连续三次双数则进监狱。
- 经过起点发放金币奖励。

### 1.3 地块类型

| 地块类型 | 说明 |
|---------|------|
| **起点 (start)** | 经过或落在起点发放奖励金币 |
| **算术 (math)** | 随机出一道 100 以内的加减法。加法的和 < 100，减法不能为负数，被减数和减数都不超过 100。答对获得地块；第二次进入自己的地块答对后可盖房/升级 |
| **英语 (english)** | 随机从词语库提取一个英文单词及图片，需通过麦克风念出正确发音。答对获得/升级地块 |
| **语文 (chinese)** | 随机从词语库提取一个中文词语，电脑念出声音，玩家从 4 个候选字里选出词语中缺掉的字。答对获得/升级地块 |
| **宝藏 (treasure)** | 随机获得 10～40 金币 |
| **休息 (rest)** | 休息，恢复少量金币 |
| **传送 (portal)** | 随机传送到棋盘上另一个地块 |
| **机会 (chance)** | 抽取机会卡，获得金币/移动/传送效果 |
| **纳税 (tax)** | 缴纳 10～25 金币 |
| **监狱 (jail)** | 角落地块，进入后跳过一回合 |

### 1.4 地块归属与房屋

- 算术/英语/语文地块可被玩家**认领（claim）**。
- 第一次答对该地块题目 → 获得地块所有权。
- 第二次起（同一玩家再进入自己的地块）→ 答对后可**盖房子或升级房子**。
- 房屋等级 0～3 级，等级越高租金越贵。
- 落在别人的地块 → 支付租金给地主。

### 1.5 经济系统

- 初始金币：每人 200 金币。
- 经过起点奖励：20 金币。
- 租金 = 基础租金 × 2^房屋等级（基础租金 = 地块价格 ÷ 5 向上取整）。
- 升级费用 = 地块价格 × (0.5 + 0.25 × 当前等级) 向上取整。
- 金币 < 0 即破产，退出游戏。
- 最后存活玩家获胜。

### 1.6 存档

- 支持 **localStorage 存档**，可随时保存当前游戏进度。
- 启动游戏时若有存档，显示"继续游戏"按钮。
- 存档内容：主题、玩家、地块状态、当前回合等全部状态。

---

## 二、视觉设计

### 2.1 主题风格

提供两个可切换的场景主题，童趣十足：

1. **冰雪城堡 (ice-castle)**
   - 主色：冰蓝 `#b0e2ff`、白色 `#ffffff`
   - 辅色：浅紫 `#d4c5f9`
   - 背景：冰蓝渐变 + 雪花飘落装饰

2. **花园 (garden)**
   - 主色：粉色 `#ffb6c1`、嫩绿 `#98fb98`
   - 辅色：鹅黄 `#fffacd`
   - 背景：粉绿渐变 + 花瓣飘落装饰

### 2.2 角色形象

四个卡通动物角色，用 emoji + 颜色区分：
- 🐰 小兔子（粉色系）
- 🦊 小狐狸（橙色系）
- 🐻 小熊（棕色系）
- 🐱 小猫咪（紫色系）

### 2.3 字体

- 主字体：`'Comic Sans MS', '幼圆', 'YouYuan', sans-serif`
- 圆角风格，柔和阴影，色彩明快。

### 2.4 图片物料生成方案

- 英文单词配图：使用 AI 图片生成 API，根据单词生成卡通插画。
- 地块图标：使用 emoji + CSS 绘制，无需额外图片资源。
- 角色形象：使用 emoji 作为头像。
- 背景：CSS 渐变 + 装饰元素，不依赖外部图片。

---

## 三、技术方案

### 3.1 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 游戏引擎 | Phaser 3 | 2D 渲染、场景管理、动画、输入 |
| 语言 | TypeScript | 类型安全 |
| 构建 | Vite | 快速开发服务器 + 打包 |
| HUD/菜单 | DOM + CSS | 文字密集界面用 DOM，canvas 只负责棋盘 |
| 存档 | localStorage | 浏览器本地存档 |
| 语音 | Web Speech API | SpeechRecognition（英语识别）+ SpeechSynthesis（中文朗读）|

### 3.2 项目结构

```
kids-monopoly/
├── index.html              # 入口 HTML
├── package.json
├── tsconfig.json
├── vite.config.ts
├── src/
│   ├── main.ts             # Phaser 游戏实例入口
│   ├── config/
│   │   └── gameConfig.ts   # Phaser 配置、常量
│   ├── types/
│   │   └── index.ts         # 类型定义
│   ├── data/
│   │   ├── tileLayout.ts   # 44 个地块布局
│   │   ├── englishWords.ts  # 英语单词库
│   │   └── chineseWords.ts  # 中文词语库
│   ├── utils/
│   │   ├── boardMath.ts     # 棋盘坐标计算
│   │   └── levenshtein.ts   # 字符串模糊匹配
│   ├── systems/            # 游戏逻辑层（不依赖 Phaser）
│   │   ├── GameState.ts
│   │   ├── DiceSystem.ts
│   │   ├── BoardSystem.ts
│   │   ├── MoveSystem.ts
│   │   ├── TileEventSystem.ts
│   │   ├── EconomySystem.ts
│   │   ├── QuizSystem.ts
│   │   ├── VoiceSystem.ts
│   │   ├── SaveSystem.ts
│   │   └── TurnSystem.ts
│   ├── scenes/             # Phaser 场景层（渲染+输入）
│   │   ├── BootScene.ts
│   │   ├── MenuScene.ts
│   │   └── GameScene.ts
│   ├── ui/                 # DOM UI 控制器
│   │   ├── HUDManager.ts
│   │   ├── TopBar.ts
│   │   ├── DicePanel.ts
│   │   ├── ChallengePanel.ts
│   │   └── MenuPanel.ts
│   └── styles/
│       ├── main.css
│       ├── themes.css
│       └── hud.css
```

### 3.3 架构原则

1. **逻辑与渲染分离**：`systems/` 只管游戏规则（纯 TypeScript），`scenes/` 只管渲染和输入。
2. **单例 GameState**：全局状态容器，所有系统共享。
3. **DOM HUD**：文字密集的界面（题目、菜单、状态栏）用 DOM，canvas 只画棋盘和角色。
4. **无循环依赖**：系统间依赖单向。

---

## 四、开发任务清单

1. 搭建项目骨架（package.json / tsconfig / vite / index.html）
2. 类型定义与数据文件（地块布局、词语库）
3. 工具函数（棋盘坐标、模糊匹配）
4. 10 个游戏系统
5. Phaser 场景（Boot / Menu / Game）
6. DOM UI 控制器（HUD / 题目面板 / 菜单）
7. CSS 样式（主样式 / 主题 / HUD）
8. 依赖安装 + TypeScript 编译验证
9. 浏览器试玩 + 修复
10. Git 提交推送
